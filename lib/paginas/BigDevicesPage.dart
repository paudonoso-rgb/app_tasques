import 'package:flutter/material.dart';

class Bigdevicespage extends StatelessWidget {
  const Bigdevicespage({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: Column(
        children: [
          Center(child: Text("Big Devices Page", style: TextStyle(fontSize: 24))),
        ],
      ),
    );
  }
}