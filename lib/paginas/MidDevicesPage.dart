import 'package:flutter/material.dart';

class Middevicespage extends StatelessWidget {
  const Middevicespage({super.key});

  @override
  Widget build(BuildContext context) {
    return  Scaffold(
      body:Column(
        children:[
          Center(child: Text("Mid Devices Page", style: TextStyle(fontSize: 24))),
        ],
      ),
    );
  }
}